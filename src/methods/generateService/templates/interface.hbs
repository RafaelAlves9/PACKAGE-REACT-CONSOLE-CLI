import { T{{name}}Response, T{{name}}PaginationResponse } from '@response/{{name}}.response.ts';
import { T{{name}}Request, T{{name}}UpdateRequest, T{{name}}PaginationRequest } from '@request/{{name}}.request.ts';

export interface I{{name}}Service {
    create(data: T{{name}}Request): Promise<boolean>;
    update(data: T{{name}}UpdateRequest): Promise<boolean>;
    delete(id: number): Promise<boolean>;
    getById(id: number): Promise<T{{name}}Response>;
    getPagination(data: T{{name}}PaginationRequest): Promise<T{{name}}PaginationResponse>;
}