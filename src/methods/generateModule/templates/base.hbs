import Use{{name}}Model from './{{name}}.model';
import {{name}}View from './{{name}}.view';

const {{name}} = () => {
   const model = Use{{name}}Model();

   return <{{name}}View {...model}/>;
};

export default {{name}};
