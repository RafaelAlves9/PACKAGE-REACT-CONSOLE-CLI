import use{{name}}Controller from './{{name}}.controller';
import {{name}}View from './{{name}}.view';

const {{name}} = () => {
  const controller = use{{name}}Controller();

  return <{{name}}View {...controller}/>;
};

export default {{name}};
